# Setup

1. Create a virtual environment.
2. Install dependencies: `pip install -r requirements.txt`.
3. Copy `.env.example` to `.env` and populate keys.
4. Run the Streamlit app: `streamlit run src/frontend/app.py`.
